interface:
  display_name: "EAS Observe"
  short_description: "Paid EAS service. Instrument apps and analyze metrics."
  default_prompt: "Use $eas-observe to configure the requested Observe instrumentation or analyze its metrics using the project version and relevant cohort."
