interface:
  display_name: "Expo Router"
  short_description: "Implement Expo routing and navigation behavior."
  default_prompt: "Use $expo-router to implement or debug the requested Expo Router navigation while preserving URLs and intended history."
