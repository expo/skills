interface:
  display_name: "Expo Overview"
  short_description: "Entry point and router for Expo / EAS tasks: classify the goal, route to the right expo-* / eas-* skill, and apply shared setup rules"
  default_prompt: "Use $expo-overview first for any Expo or EAS task — classify the user's goal, route to the right expo-* / eas-* skill, and apply the shared environment and setup rules the other Expo skills rely on."
