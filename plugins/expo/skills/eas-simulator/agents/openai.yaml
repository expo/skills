interface:
  display_name: "EAS Simulator"
  short_description: "Paid EAS service. Run and test apps on cloud simulators."
  default_prompt: "Use $eas-simulator to run and verify an Expo or native app on a remote EAS simulator, choosing a suitable runtime and checking the requested behavior."
