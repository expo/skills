interface:
  display_name: "EAS Simulator"
  short_description: "Paid EAS service. Test apps on remote simulators."
  default_prompt: "Use $eas-simulator to run the requested remote device test with a matching build, track session ownership, and clean up task-owned resources."
