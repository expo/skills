interface:
  display_name: "Expo UI"
  short_description: "Integrate universal or platform-specific Expo UI."
  default_prompt: "Use $expo-ui to choose or integrate the appropriate @expo/ui layer for the installed SDK, preserving existing UI choices and platform behavior."
