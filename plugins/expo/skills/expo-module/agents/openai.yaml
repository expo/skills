interface:
  display_name: "Expo Module"
  short_description: "Build Expo native modules and views."
  default_prompt: "Use $expo-module to implement or review the requested native module, view, or config plugin and verify its native integration."
