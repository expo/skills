interface:
  display_name: "Expo Skill Feedback"
  short_description: "Expo feedback, eval candidates, and opt-in telemetry."
  default_prompt: "Use $expo-skill-feedback to prepare actionable Expo feedback or handle the requested telemetry change, preserving consent and opt-in defaults."
